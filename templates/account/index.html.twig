{% extends 'base.html.twig' %}

{% block title %}Mon espace personnel{% endblock %}

{% block body %}
<div>
    <h1>Bonjour {{ username }} !</h1>

    <ul>
        {% for animal in animals %}
            <li>
                <h2>{{ animal.name }}</h2>
                <a class="button" href="/carnet/{{ animal.id }}">Carnet de {{ animal.name }}</a>
                <a class="button" href="/visit/{{animal.id}}">Visites de {{ animal.name }}</a>
            </li>
        {% endfor %}
    </ul>

    <a class="button bg_white" href="/logout">Se déconnecter</a>
</div>
{% endblock %}
